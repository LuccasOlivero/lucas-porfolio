import styled from "styled-components";

export const Paragraph = styled.p`
  color: var(--color-main);
  /* max-width: 800px; */
  text-wrap: balance;
  font-size: 1.5rem;
  line-height: 2rem;
  margin-top: 1rem;
`;
