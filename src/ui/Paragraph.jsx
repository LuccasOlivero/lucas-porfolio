import styled from "styled-components";

export const Paragraph = styled.p`
  color: var(--color-main);
  max-width: 800px;
  text-wrap: balance;
  font-size: 1em;
  line-height: 1.4rem;
  margin-top: 1rem;
`;
